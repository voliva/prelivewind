import { h } from 'preact';
import './stationSummary.css';
import Station from '../services/station';

const StationSummary = ({station}:{station: Station}) => {
    
    return <div>StationSummary</div>;
}

export default StationSummary;